#include <stdlib.h>
#include <stdio.h>
#include "read_graph_from_file1.h"

void create_SNN_graph1();

int main(int argc, char const *argv[]) {
  /* code */
  return 0;
}

void create_SNN_graph1(int N, char **table2D, int ***SNN_table){

  *SNN_table = (char **) calloc(*N, sizeof *SNN_table);
  (*SNN_table)[0] = (char*) calloc(*N * *N, sizeof (*SNN_table)[0]);

         }
     }


   }
   }
 }
//table use a 2D array of int values to record the numbers of SNNs
  return;
}
